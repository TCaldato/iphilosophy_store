from django.apps import AppConfig


class AboutConfig(AppConfig):
    """
    This class configures settings specific to the 'About' app
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "about"
